# Milana Premium Catalog Website

GitHub Pages ready.

## This version

- The 4 Google Drive PDF links are saved directly in `index.html`.
- The hidden PDF update/edit panel was removed.
- PDF links work on all devices.
- Thumbnails use English and Uzbek versions. Russian uses the English thumbnail fallback.

## Upload to GitHub

Replace these in the repository root:

- `index.html`
- `covers/`
- `README.md`

The `pdfs/` folder is not required because Google Drive links are used.
